{% extends 'base.html.twig' %}

{% block title %}Responsable{% endblock %}

{% block body %}
    <h1>Responsable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ responsable.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ responsable.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_responsable_index') }}">back to list</a>

    <a href="{{ path('app_admin_responsable_edit', {'id': responsable.id}) }}">edit</a>

    {{ include('admin_responsable/_delete_form.html.twig') }}
{% endblock %}
